# Audio Assets

Place optional ambient/alert audio files here:
- `ambient.mp3` — background neural hum
- `alert.mp3` — attack detected sound
- `heal.mp3` — stabilization sound

These are optional. The app runs without them.
